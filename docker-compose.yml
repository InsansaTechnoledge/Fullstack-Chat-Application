version: '3.8'
services:
  api:
    build: ./server
    ports:
      - 5000:5000
    environment:
      - PORT=5000
      - AWS_REGION=us-east-1
      - DYNAMODB_USERS_TABLE=Users
      - DYNAMODB_MESSAGES_TABLE=Messages
